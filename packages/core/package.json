{
  "name": "@courtvision/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain logic: state machine, event kinds, sport profiles, commentary phrasebook. Pure TypeScript — no React, no DOM, no React Native imports.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
