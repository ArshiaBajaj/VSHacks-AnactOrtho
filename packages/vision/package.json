{
  "name": "@courtvision/vision",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic vision math: homography estimation, kinematic ball predictor, jump/release trackers, ball-color prior. Runs identically in native (via jsc/hermes) or the web PWA.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@courtvision/core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
