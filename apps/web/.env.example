# Web workspace environment (all optional)

# Base URL of the Anact Ortho backend (apps/server).
# Defaults to http://localhost:8787 when unset.
VITE_API_URL=http://localhost:8787

# Supabase (HooperIQ persistence) — filled by ./hooperiq/create-supabase.sh
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
