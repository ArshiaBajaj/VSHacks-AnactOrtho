{
  "name": "@courtvision/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Anact Ortho backend — real NBA data feed, film-room replay timelines, scout-card persistence, and optional LLM-powered commentary/scouting reports. Runs standalone; the app also works fully offline without it.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "start": "tsx --env-file=.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "serve": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^24.13.3",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
